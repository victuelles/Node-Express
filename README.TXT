mkdir library

cd library

library$ 
touch app.js //create app.js


npm init // setup npm create package.json

sudo npm install express --save // save 

bower // package manager for the web
npm // backend package manager

//creates bower.json

$ sudo npm install  bower  -g

$ bower init

$ bower install --save bootstrap        // installs dependencies jquery on bower.json

  "dependencies": {
    "bootstrap": "^3.3.7"
  }
}


gulp -- configuring

enforcing coding standards

injecting css and js references

Auto restarting our application (no need to restart server)


Gulp- Task manager for web projects (build tool)
  - Installed with NPM
  - Easy to use
  - Code based configuration
  - Package based (inject or wiredep, wrap it with gulp)

 JS Hint - code quality enforcement
 -detect potential erros
 -eonforce coding conventions
 - easity configuration
 - Open source

 JSCS -Code style enforcement
  - enforces style conventions
  - easily configuration

Download . files
  https://github.com/jonathanfmills/CodingStandards



sudo npm install -g gulp

 npm install gulp --save-dev


 //create gulpfile.js

 sudo npm install --save-dev gulp-jshint gulp-jscs jshint-stylish


 What is templating engine?

 Jade - templating engine that has to be compiled.
 Handlebars-
 EJS - will use on the project